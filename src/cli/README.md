# CLI

The AlgoVerse CLI lives at the **repository root**:

```text
../../cli/            # `algoverse run <script.py>`
```

```bash
npm run algoverse -- run examples/bubble.py
```

This folder exists so the architecture layout is discoverable from `src/`.
